# Database Configuration
# SQLite database is used by default (see server/db.js)
# No additional configuration needed for local development

# Google OAuth Configuration
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# App Configuration
VITE_APP_NAME=Life Clinic Management System
VITE_REDIRECT_DELAY_MS=5000

# Live Application URL
VITE_LIVE_URL=https://life-clinic-management-system.vercel.app

# Email Configuration (SMTP)
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password
SMTP_FROM=Life Clinic Management System <noreply@clinic.com>
FRONTEND_URL=http://localhost:5173
